# Day AI OAuth Credentials
# Run 'yarn oauth:setup' from the root of the repo to get these
CLIENT_ID=
CLIENT_SECRET=
REFRESH_TOKEN=
DAY_AI_BASE_URL=https://day.ai

# OpenWeather API Key (free tier)
# Get yours at: https://openweathermap.org/api
OPENWEATHER_API_KEY=

# Location for weather updates
# Format: "City, State" or "City, Country"
LOCATION=San Francisco, CA

# Cron secret for securing the endpoint (optional but recommended)
# Generate with: openssl rand -base64 32
CRON_SECRET=

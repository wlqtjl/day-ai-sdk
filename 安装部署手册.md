# Day AI SDK 安装部署手册

## 1. 环境准备

### 1.1 系统要求

| 平台 | 最低要求 | 推荐配置 |
|------|---------|---------|
| **Windows** | Windows 10 或 Windows 11 | Windows 11, 8GB+ RAM, SSD |
| **macOS** | macOS 10.15 (Catalina) | macOS 13+ (Ventura), 8GB+ RAM, SSD |
| **Linux** | Ubuntu 20.04 或 CentOS 8 | Ubuntu 22.04, 8GB+ RAM, SSD |

### 1.2 软件依赖

| 软件 | 版本要求 | 用途 |
|------|---------|------|
| **Node.js** | 16.0 或更高 | 运行环境 |
| **npm** | 8.0 或更高 | 包管理 |
| **yarn** | 1.22 或更高 | 包管理（可选） |
| **Git** | 2.0 或更高 | 代码版本控制 |
| **Python** | 3.8 或更高 | 微信聊天记录收集（可选） |

### 1.3 网络要求

- 能够访问 Day AI 平台（https://day.ai）
- 能够访问 GitHub（https://github.com）
- 能够访问 Claude AI API（可选）
- 能够访问国产大模型 API（可选）

## 2. 安装步骤

### 2.1 克隆代码

```bash
# 克隆代码库
git clone https://github.com/wlqtjl/day-ai-sdk

# 进入项目目录
cd day-ai-sdk
```

### 2.2 安装依赖

#### 使用 npm

```bash
# 安装项目依赖
npm install

# 构建 SDK
npm run build
```

#### 使用 yarn

```bash
# 安装项目依赖
yarn install

# 构建 SDK
yarn build
```

### 2.3 配置 OAuth

1. **复制环境变量模板**

```bash
# 复制 .env.example 到 .env
cp .env.example .env
```

2. **编辑 .env 文件**

```bash
# 打开 .env 文件进行编辑
# 设置 INTEGRATION_NAME 为你的集成名称
INTEGRATION_NAME=My Custom Integration

# 其他配置会在 OAuth 授权后自动填充
```

3. **运行 OAuth 向导**

```bash
# 运行 OAuth 向导
npm run oauth:setup
# 或
yarn oauth:setup
```

4. **完成浏览器授权**

- 向导会打开浏览器，跳转到 Day AI 授权页面
- 登录你的 Day AI 账号
- 授权应用访问你的 Day AI 数据
- 授权完成后，向导会自动保存凭证到 .env 文件

### 2.4 运行示例应用

#### 桌面应用

```bash
# 进入桌面应用目录
cd examples/desktop

# 安装依赖
npm install
# 或
yarn install

# 运行开发服务器
npm run dev

# 运行 Electron 应用
npm run electron:dev
```

#### 移动应用

```bash
# 进入移动应用目录
cd examples/mobile

# 安装依赖
npm install
# 或
yarn install

# 运行 iOS 模拟器
npm run ios

# 运行 Android 模拟器
npm run android

# 运行 Web 版本
npm run web
```

#### Web 应用

```bash
# 进入 Web 应用目录
cd examples/vercel-weather-cron

# 安装依赖
npm install
# 或
yarn install

# 运行开发服务器
npm run dev
```

## 3. 配置指南

### 3.1 环境变量配置

| 变量名 | 描述 | 默认值 | 必填 |
|-------|------|-------|------|
| `INTEGRATION_NAME` | 你的集成名称 | - | 是 |
| `DAY_AI_BASE_URL` | Day AI 实例 URL | `https://day.ai` | 否 |
| `CLIENT_ID` | OAuth 客户端 ID | - | 是（由向导自动填充） |
| `CLIENT_SECRET` | OAuth 客户端密钥 | - | 是（由向导自动填充） |
| `REFRESH_TOKEN` | OAuth 刷新令牌 | - | 是（由向导自动填充） |
| `ANTHROPIC_API_KEY` | Claude AI API 密钥 | - | 否（使用 Claude 时需要） |
| `DOUBAO_API_KEY` | 豆包 API 密钥 | - | 否（使用豆包时需要） |
| `QIANWEN_API_KEY` | 千问 API 密钥 | - | 否（使用千问时需要） |

### 3.2 微信集成配置

#### 安装 PyWxDump（可选）

```bash
# 安装 PyWxDump
pip install pywxdump

# 或从源码安装
git clone https://github.com/LC044/WeChatMsg
go WeChatMsg
pip install -e .
```

#### 配置微信路径

在 `examples/desktop/electron/services/ToolExecutor.ts` 中，你可以修改微信数据路径的检测逻辑：

```typescript
// 检测微信数据路径
switch (detectedPlatform) {
  case 'darwin': // macOS
    {
      const homeDir = os.homedir()
      const possiblePaths = [
        path.join(homeDir, 'Library', 'Containers', 'com.tencent.xinWeChat', 'Data', 'Library', 'Application Support', 'com.tencent.xinWeChat'),
        path.join(homeDir, 'Documents', 'WeChat Files')
      ]
      // 添加自定义路径
      possiblePaths.push('/path/to/your/wechat/data')
      
      for (const p of possiblePaths) {
        if (fs.existsSync(p)) {
          wechatPath = p
          break
        }
      }
    }
    break;
  // 其他平台...
}
```

### 3.3 AI 模型配置

在 `examples/desktop/electron/main.ts` 中，你可以配置默认的 AI 模型：

```typescript
function getAgentService(): AgentService {
  const config = loadConfig()
  const llmType = config.llmType || 'claude' // 可以修改为 'doubao' 或 'qianwen'
  let apiKey: string

  switch (llmType) {
    case 'claude':
      apiKey = config.anthropicApiKey || ''
      if (!apiKey) {
        throw new Error('Anthropic API key not configured')
      }
      break
    case 'doubao':
      apiKey = config.doubaoApiKey || ''
      if (!apiKey) {
        throw new Error('Doubao API key not configured')
      }
      break
    case 'qianwen':
      apiKey = config.qianwenApiKey || ''
      if (!apiKey) {
        throw new Error('Qianwen API key not configured')
      }
      break
    default:
      throw new Error(`Unsupported LLM type: ${llmType}`)
  }

  if (!agentService) {
    agentService = new AgentService(apiKey, llmType)
  }
  return agentService
}
```

## 4. 部署方法

### 4.1 桌面应用打包

#### 使用 electron-builder

1. **安装 electron-builder**

```bash
# 进入桌面应用目录
cd examples/desktop

# 安装 electron-builder
npm install electron-builder --save-dev
# 或
yarn add electron-builder --dev
```

2. **修改 package.json**

在 `examples/desktop/package.json` 中添加打包配置：

```json
"build": {
  "appId": "com.dayai.desktop-demo",
  "productName": "Day AI Demo",
  "mac": {
    "target": [
      "dmg",
      "zip"
    ],
    "category": "public.app-category.productivity"
  },
  "win": {
    "target": [
      "nsis",
      "portable"
    ]
  },
  "linux": {
    "target": [
      "AppImage",
      "deb",
      "rpm"
    ]
  },
  "files": [
    "dist/**/*",
    "dist-electron/**/*"
  ]
}
```

3. **执行打包**

```bash
# 构建应用
npm run build

# 打包应用
npx electron-builder
```

4. **获取安装包**

打包完成后，安装包会生成在 `examples/desktop/dist` 目录中。

### 4.2 Web 应用部署

#### 部署到 Vercel

1. **安装 Vercel CLI**

```bash
# 安装 Vercel CLI
npm install -g vercel
# 或
yarn global add vercel
```

2. **部署应用**

```bash
# 进入 Web 应用目录
cd examples/vercel-weather-cron

# 登录 Vercel
vercel login

# 部署应用
vercel
```

3. **配置环境变量**

在 Vercel 仪表板中配置环境变量：
- `INTEGRATION_NAME`
- `CLIENT_ID`
- `CLIENT_SECRET`
- `REFRESH_TOKEN`
- 其他必要的 API 密钥

### 4.3 移动应用部署

#### iOS 部署

1. **生成 iOS 构建**

```bash
# 进入移动应用目录
cd examples/mobile

# 安装依赖
npm install

# 生成 iOS 构建
eas build:ios
```

2. **使用 Xcode 部署**

- 打开 `examples/mobile/ios` 目录中的 Xcode 项目
- 配置证书和配置文件
- 构建并部署到 App Store

#### Android 部署

1. **生成 Android 构建**

```bash
# 进入移动应用目录
cd examples/mobile

# 安装依赖
npm install

# 生成 Android 构建
eas build:android
```

2. **使用 Android Studio 部署**

- 打开 `examples/mobile/android` 目录中的 Android Studio 项目
- 配置签名密钥
- 构建并部署到 Google Play Store

## 5. 常见问题及解决方案

### 5.1 安装问题

#### Node.js 未安装

**问题**：运行 `npm` 或 `yarn` 命令时提示 "command not found"

**解决方案**：
- 访问 https://nodejs.org/en/download/ 下载并安装 Node.js
- 或使用包管理器安装：
  - Windows: `winget install OpenJS.NodeJS`
  - macOS: `brew install node`
  - Linux: `sudo apt install nodejs npm`

#### 依赖安装失败

**问题**：运行 `npm install` 或 `yarn install` 时失败

**解决方案**：
- 清除缓存：`npm cache clean --force` 或 `yarn cache clean`
- 删除 `node_modules` 目录：`rm -rf node_modules`
- 重新安装依赖：`npm install` 或 `yarn install`

### 5.2 OAuth 配置问题

#### 授权超时

**问题**：运行 `npm run oauth:setup` 时提示 "Authorization timeout"

**解决方案**：
- 确保在 5 分钟内完成浏览器授权
- 检查网络连接是否正常
- 确保 Day AI 平台可访问

#### 凭证错误

**问题**：运行应用时提示 "Missing required OAuth credentials"

**解决方案**：
- 确保 `.env` 文件存在且包含正确的凭证
- 重新运行 `npm run oauth:setup` 完成授权
- 检查 `CLIENT_ID`、`CLIENT_SECRET` 和 `REFRESH_TOKEN` 是否正确

### 5.3 运行问题

#### 沙盒初始化失败

**问题**：运行 Electron 应用时提示 "sandbox initialization failed: Operation not permitted"

**解决方案**：
- 在 `package.json` 中修改 `electron:dev` 脚本，添加 `--no-sandbox` 参数：
  ```json
  "electron:dev": "electron --no-sandbox ."
  ```
- 在 `electron/main.ts` 中的 `webPreferences` 中添加 `sandbox: false`

#### 微信聊天记录收集失败

**问题**：调用 `collect_wechat_messages` 工具时失败

**解决方案**：
- 确保微信已安装并登录
- 确保用户有微信数据目录的访问权限
- 安装 PyWxDump 工具：`pip install pywxdump`
- 检查微信数据路径是否正确

#### AI 模型调用失败

**问题**：调用 AI 模型时提示 "API key not configured"

**解决方案**：
- 确保在 `.env` 文件中配置了正确的 API 密钥
- 对于 Claude AI，设置 `ANTHROPIC_API_KEY`
- 对于豆包，设置 `DOUBAO_API_KEY`
- 对于千问，设置 `QIANWEN_API_KEY`

### 5.4 性能问题

#### 应用启动缓慢

**问题**：应用启动时间过长

**解决方案**：
- 确保硬件满足推荐配置
- 关闭不必要的后台应用
- 清理应用缓存：删除 `.temp_userdata` 目录

#### 内存占用过高

**问题**：应用运行时内存占用过高

**解决方案**：
- 关闭不必要的标签页和窗口
- 减少同时运行的应用数量
- 增加系统内存（如果可能）

## 6. 故障排查

### 6.1 日志查看

#### 桌面应用日志

- **开发模式**：在终端中查看输出
- **生产模式**：查看应用目录下的 `logs` 目录

#### 移动应用日志

- **iOS**：使用 Xcode 控制台
- **Android**：使用 Android Studio 日志猫

#### Web 应用日志

- **开发模式**：在浏览器控制台中查看
- **生产模式**：在 Vercel 仪表板中查看

### 6.2 常见错误代码

| 错误代码 | 描述 | 解决方案 |
|---------|------|---------|
| `401 Unauthorized` | 未授权访问 | 重新运行 `npm run oauth:setup` 完成授权 |
| `403 Forbidden` | 禁止访问 | 检查 API 密钥和权限设置 |
| `404 Not Found` | 资源不存在 | 检查 API 端点和参数 |
| `500 Internal Server Error` | 服务器内部错误 | 检查 Day AI 平台状态和网络连接 |
| `ECONNREFUSED` | 连接被拒绝 | 检查网络连接和 Day AI 平台状态 |

### 6.3 调试技巧

#### 启用调试模式

```bash
# 运行桌面应用并打开开发者工具
npm run electron:dev

# 在应用中按下 Cmd+Option+I (macOS) 或 Ctrl+Shift+I (Windows/Linux) 打开开发者工具
```

#### 网络请求调试

- 使用浏览器开发者工具的网络面板查看 API 请求
- 检查请求和响应的详细信息
- 验证 OAuth 令牌是否正确

#### 代码调试

- 在关键代码处添加 `console.log()` 语句
- 使用 VS Code 的调试功能
- 检查错误堆栈信息

## 7. 维护和更新

### 7.1 版本管理

#### 查看当前版本

```bash
# 查看 SDK 版本
npm version

# 查看示例应用版本
cd examples/desktop
npm version
```

#### 更新 SDK

```bash
# 拉取最新代码
git pull origin main

# 重新安装依赖
npm install

# 重新构建
npm run build
```

#### 更新示例应用

```bash
# 进入示例应用目录
cd examples/desktop

# 拉取最新代码
git pull origin main

# 重新安装依赖
npm install

# 重新构建
npm run build
```

### 7.2 备份和恢复

#### 备份配置

```bash
# 备份 .env 文件
cp .env .env.backup

# 备份应用数据
cp -r examples/desktop/.temp_userdata examples/desktop/.temp_userdata.backup
```

#### 恢复配置

```bash
# 恢复 .env 文件
cp .env.backup .env

# 恢复应用数据
cp -r examples/desktop/.temp_userdata.backup examples/desktop/.temp_userdata
```

### 7.3 安全最佳实践

- **保护 API 密钥**：不要将 API 密钥提交到版本控制系统
- **定期更新令牌**：定期运行 `npm run oauth:setup` 更新 OAuth 令牌
- **使用 HTTPS**：确保所有网络通信使用 HTTPS
- **限制权限**：只授予应用必要的权限
- **定期备份**：定期备份应用数据和配置

## 8. 技术支持

### 8.1 联系方式

- **GitHub Issues**：在 https://github.com/wlqtjl/day-ai-sdk/issues 提交问题
- **Email**：contact@day.ai
- **Discord**：Day AI 开发者社区

### 8.2 支持流程

1. **问题描述**：详细描述问题，包括错误信息、重现步骤和环境信息
2. **日志提供**：提供相关日志文件和错误堆栈
3. **版本信息**：提供 SDK 版本和示例应用版本
4. **等待响应**：技术支持团队会在 24-48 小时内响应

### 8.3 常见问题解答

- **Q**：如何获取 Day AI 平台账号？
  **A**：访问 https://day.ai 注册账号

- **Q**：如何获取 Claude AI API 密钥？
  **A**：访问 https://console.anthropic.com/ 注册并获取 API 密钥

- **Q**：如何获取豆包 API 密钥？
  **A**：访问 https://cloud.bytedance.com/ 注册并获取 API 密钥

- **Q**：如何获取千问 API 密钥？
  **A**：访问 https://dashscope.aliyun.com/ 注册并获取 API 密钥

## 9. 总结

Day AI SDK 提供了一套完整的工具和模板，帮助开发者快速构建 AI 驱动的业务应用。通过本手册的指导，你应该能够成功安装、配置和部署 Day AI SDK 及其示例应用。

如果遇到任何问题，请参考本手册的常见问题及解决方案部分，或联系技术支持团队获取帮助。

祝你使用愉快！
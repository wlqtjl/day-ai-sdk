# Day AI SDK 详细安装部署手册

## 一、项目概述

Day AI SDK 是一个功能强大的AI驱动客户关系管理工具，集成了微信聊天记录收集、国产大模型兼容、销售权限系统、隐藏录音功能等多种企业级功能。

## 二、环境要求

### 2.1 系统要求
- **操作系统**：Windows 10+、macOS 10.15+、Linux
- **Node.js**：v16.0+ 
- **npm**：v7.0+ 或 **yarn**：v1.22+ 
- **TypeScript**：v5.0+

### 2.2 硬件要求
- **CPU**：至少 4 核心
- **内存**：至少 8GB
- **磁盘空间**：至少 50GB

## 三、安装步骤

### 3.1 克隆代码库

```bash
# 克隆仓库
git clone https://github.com/wlqtjl/day-ai-sdk.git

# 进入项目目录
cd day-ai-sdk

# 切换到开发分支
git checkout dev-new-features
```

### 3.2 安装依赖

```bash
# 使用 npm 安装依赖
npm install

# 或使用 yarn 安装依赖
yarn install
```

### 3.3 编译项目

```bash
# 编译 TypeScript 代码
npm run build

# 或
yarn build
```

## 四、项目结构说明

### 4.1 核心目录结构

```
day-ai-sdk/
├── src/             # 核心源代码
│   ├── wechat-decrypt/    # 微信聊天记录解密和收集功能（新增）
│   ├── llm-domestic/      # 国产大模型集成（新增）
│   ├── ai/                # 高级AI功能（新增）
│   ├── customer-profile/  # 客户画像相关功能（新增）
│   ├── integration/       # 第三方系统集成（新增）
│   ├── security/          # 安全相关功能（新增）
│   ├── mobile/            # 移动应用增强（新增）
│   ├── dashboard/         # 仪表盘功能（新增）
│   ├── i18n/              # 多语言支持（新增）
│   ├── api/               # API 服务器
│   ├── client.ts          # 客户端核心文件
│   └── index.ts           # 主入口文件
├── examples/        # 示例应用
│   ├── desktop/           # 桌面端示例
│   ├── mobile/            # 移动端示例
│   └── vercel-weather-cron/ # Vercel 天气定时任务示例
├── scripts/         # 辅助脚本
├── tests/           # 测试文件
├── backups/         # 代码备份
├── package.json     # 项目配置
└── tsconfig.json    # TypeScript 配置
```

### 4.2 新增代码位置（重点标记）

#### 4.2.1 微信聊天记录收集功能
- **位置**：`src/wechat-decrypt/`
- **文件**：
  - `index.ts` - 核心解密逻辑
  - `monitor.ts` - 实时监控功能
  - `platforms.ts` - 多平台支持
  - `pywxdump.ts` - Python 工具集成
- **功能**：支持 Windows、macOS、Android、iOS 等多平台的微信聊天记录解密和实时收集

#### 4.2.2 国产大模型集成
- **位置**：`src/llm-domestic/`
- **文件**：
  - `index.ts` - 大模型管理器
  - `base.ts` - 基础接口
  - `doubao.ts` - 豆包模型
  - `qianwen.ts` - 千问模型
  - `xinghuo.ts` - 星火模型
  - `glm.ts` - GLM 模型
  - `multi-model.ts` - 多模型管理
- **功能**：集成多种国产大模型，支持模型自动选择和多模型投票

#### 4.2.3 高级 AI 功能
- **位置**：`src/ai/`
- **文件**：
  - `analysis/advanced-analysis.ts` - 高级分析功能
  - `models/advanced-model-manager.ts` - 高级模型管理
- **功能**：深度学习、强化学习、计算机视觉等高级AI能力

#### 4.2.4 客户画像系统
- **位置**：`src/customer-profile/`
- **文件**：
  - `ai-analysis/` - AI 分析功能（销售预测、话术推荐、竞争对手分析）
  - `analytics/` - 数据分析（销售漏斗分析）
  - `data-integration/` - 数据集成（邮件、社交媒体）
  - `industry-templates/` - 行业模板
  - `lifecycle/` - 客户生命周期管理
  - `models/` - 数据模型
  - `visualization/` - 数据可视化
- **功能**：360度客户视图、智能分析、个性化推荐

#### 4.2.5 第三方系统集成
- **位置**：`src/integration/`
- **文件**：
  - `crm/index.ts` - CRM 系统集成
  - `email/index.ts` - 邮件系统集成
  - `social-media/index.ts` - 社交媒体集成
- **功能**：与各种CRM、邮件和社交媒体平台的集成

#### 4.2.6 安全功能
- **位置**：`src/security/`
- **文件**：`index.ts`
- **功能**：用户认证、权限管理、数据加密

#### 4.2.7 移动应用增强
- **位置**：`src/mobile/`
- **文件**：`enhancements/index.ts`
- **功能**：离线数据支持、推送通知、性能优化

#### 4.2.8 隐藏录音功能
- **位置**：`examples/mobile/src/`
- **文件**：
  - `components/VoiceRecorder.tsx` - 录音组件
  - `services/VoiceRecordService.ts` - 录音服务
- **功能**：支持销售员秘密记录客户对话并进行AI分析

## 五、配置说明

### 5.1 环境变量配置

复制 `.env.example` 文件为 `.env` 并填写相关配置：

```bash
cp .env.example .env
```

主要配置项：

- **API_KEYS**：各种API密钥
- **WECHAT_CONFIG**：微信相关配置
- **LLM_CONFIG**：大模型配置
- **SECURITY_CONFIG**：安全相关配置

### 5.2 权限配置

在 `src/security/index.ts` 中配置销售权限系统：

- **销售总监**：拥有所有权限
- **销售经理**：管理团队内销售员，查看团队所有数据
- **销售员工**：只能查看和管理自己的客户数据

## 六、运行和部署

### 6.1 开发模式

```bash
# 启动开发服务器
npm run dev

# 或
yarn dev
```

### 6.2 桌面应用

```bash
# 进入桌面示例目录
cd examples/desktop

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 或构建应用
npm run build
npm run electron:build
```

### 6.3 移动端应用

```bash
# 进入移动示例目录
cd examples/mobile

# 安装依赖
npm install

# 启动开发服务器
npm start

# 或构建应用
npm run build
```

### 6.4 生产部署

1. **编译代码**：`npm run build`
2. **部署API服务器**：使用 PM2 或其他进程管理工具
3. **部署前端应用**：使用 Nginx 或其他 web 服务器
4. **配置域名和SSL**：确保安全访问

## 七、常见问题和解决方案

### 7.1 微信聊天记录收集问题

**问题**：无法找到微信数据路径
**解决方案**：检查 `src/wechat-decrypt/platforms.ts` 中的路径配置，根据不同操作系统调整

**问题**：解密失败
**解决方案**：确保微信版本兼容，尝试使用 `pywxdump` 工具辅助解密

### 7.2 大模型调用问题

**问题**：API 密钥配置错误
**解决方案**：检查 `.env` 文件中的 API 密钥配置

**问题**：模型响应慢
**解决方案**：调整模型参数，使用多模型投票机制提高响应速度

### 7.3 权限系统问题

**问题**：权限配置不生效
**解决方案**：检查 `src/security/index.ts` 中的权限配置，确保角色分配正确

### 7.4 移动应用问题

**问题**：录音功能无法使用
**解决方案**：确保应用有录音权限，检查 `VoiceRecordService.ts` 中的配置

**问题**：离线功能不工作
**解决方案**：检查 `src/mobile/enhancements/index.ts` 中的离线数据配置

## 八、代码备份和版本管理

### 8.1 代码备份

项目自动在 `backups/` 目录创建版本备份：
- `day-ai-sdk-v1.2.0.tar.gz` - 当前版本备份

### 8.2 版本管理

- **v0.2.0**：基础版本
- **v1.0.0**：添加微信数据收集
- **v1.1.0**：添加国产大模型兼容
- **v1.2.0**：添加销售权限系统、隐藏录音功能等完整功能

## 九、技术支持

### 9.1 联系方式

- **GitHub Issues**：https://github.com/wlqtjl/day-ai-sdk/issues
- **技术文档**：项目根目录下的 `产品介绍文档.md`、`知识问答文档.md`

### 9.2 常见错误排查

1. **构建失败**：检查 TypeScript 配置和依赖版本
2. **运行时错误**：检查环境变量配置和 API 密钥
3. **权限错误**：检查文件权限和系统权限设置

## 十、总结

Day AI SDK 是一个功能强大的AI驱动客户关系管理工具，通过集成微信聊天记录收集、国产大模型、销售权限系统、隐藏录音等功能，为企业提供了全面的客户管理解决方案。

按照本手册的步骤进行安装和部署，即可快速启动和使用系统的全部功能。如果遇到问题，请参考常见问题部分或联系技术支持。
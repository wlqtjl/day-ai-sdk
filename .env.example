# Day AI SDK Configuration
# Copy this file to .env and fill in your values

# Your integration name - this will be used as the OAuth client name
INTEGRATION_NAME=My Day AI Integration

# Day AI instance URL (use local for development)
DAY_AI_BASE_URL=https://day.ai
# DAY_AI_BASE_URL=http://localhost:8910  # For local development

# Optional: OAuth callback URL (only change if you need a different host/port)
# CALLBACK_URL=http://127.0.0.1:8080/callback

# OAuth credentials (will be populated after running the OAuth setup script)
CLIENT_ID=
CLIENT_SECRET=
REFRESH_TOKEN=

# Optional: Workspace ID to target (leave empty to use default/first available)
WORKSPACE_ID=
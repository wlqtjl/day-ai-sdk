# Day AI SDK 知识问答文档

## 1. 产品概览

### 1.1 什么是 Day AI SDK？

**Q**: Day AI SDK 是什么？它的主要用途是什么？

**A**: Day AI SDK 是一套用于构建 AI 驱动的业务工具的开发套件，帮助开发者在几分钟内而非几个月内构建与 Day AI 平台集成的应用程序。它的主要用途包括：
- 快速集成 Day AI 平台的 CRM 功能
- 构建具有 AI 能力的业务应用
- 收集和分析微信聊天记录
- 集成国产大模型

### 1.2 Day AI SDK 有哪些核心功能？

**Q**: Day AI SDK 提供哪些核心功能？

**A**: Day AI SDK 提供以下核心功能：
- **MCP 客户端**：完整的 Model Context Protocol 实现，与 Day AI 平台无缝集成
- **多平台支持**：提供桌面、移动和 Web 端的示例模板
- **AI 集成**：集成 Claude AI 和国产大模型（豆包、千问）
- **微信集成**：支持多平台微信聊天记录收集和分析
- **行业特定功能**：为超融合基础设施行业提供特定功能

### 1.3 Day AI SDK 适用于哪些场景？

**Q**: Day AI SDK 适用于哪些应用场景？

**A**: Day AI SDK 适用于以下场景：
- **客户关系管理**：快速构建与 Day AI CRM 集成的自定义应用
- **业务分析**：通过 AI 分析业务数据，生成洞察
- **自动化工作流**：创建自动化的业务流程和提醒
- **团队协作**：构建团队协作工具，提升工作效率
- **微信数据分析**：收集和分析微信聊天记录，提取业务价值

## 2. 技术实现

### 2.1 MCP 协议是什么？

**Q**: MCP 协议是什么？它在 Day AI SDK 中的作用是什么？

**A**: MCP (Model Context Protocol) 是一种模型上下文协议，用于 AI 模型与外部系统之间的通信。在 Day AI SDK 中，MCP 协议用于与 Day AI 平台进行通信，使 AI 模型能够调用 Day AI 平台提供的工具，如搜索联系人、创建机会、获取会议记录等。

### 2.2 如何集成微信聊天记录收集？

**Q**: Day AI SDK 如何实现微信聊天记录的收集？

**A**: Day AI SDK 通过以下方式实现微信聊天记录的收集：
- **多平台支持**：支持 macOS、Windows、Android、iOS 等平台
- **数据路径检测**：自动检测微信数据存储路径
- **第三方工具集成**：集成 PyWxDump 等工具进行微信数据解密
- **实时监控**：支持实时监控微信数据库文件变化
- **模拟数据**：当无法获取真实数据时，提供模拟数据

### 2.3 如何集成国产大模型？

**Q**: Day AI SDK 如何集成豆包、千问等国产大模型？

**A**: Day AI SDK 通过以下方式集成国产大模型：
- **统一接口**：提供统一的 LLMClient 接口，支持不同模型的切换
- **模型实现**：分别实现 DoubaoClient 和 QianwenClient 类
- **多模型管理**：支持模型选择和协作
- **中文优化**：针对中文对话进行优化

### 2.4 如何实现 OAuth 认证？

**Q**: Day AI SDK 如何实现 OAuth 认证？

**A**: Day AI SDK 通过以下步骤实现 OAuth 认证：
1. **动态客户端注册**：自动注册 OAuth 客户端
2. **浏览器授权**：打开浏览器进行用户授权
3. **令牌管理**：自动处理令牌刷新和存储
4. **安全存储**：将凭证安全存储在 .env 文件中

## 3. 安装和配置

### 3.1 如何安装 Day AI SDK？

**Q**: 如何安装 Day AI SDK？

**A**: 安装 Day AI SDK 的步骤如下：
1. **克隆代码**：`git clone https://github.com/wlqtjl/day-ai-sdk`
2. **进入目录**：`cd day-ai-sdk`
3. **安装依赖**：`npm install` 或 `yarn install`
4. **构建 SDK**：`npm run build` 或 `yarn build`
5. **配置 OAuth**：运行 `npm run oauth:setup` 完成授权

### 3.2 如何配置环境变量？

**Q**: 如何配置 Day AI SDK 的环境变量？

**A**: 配置环境变量的步骤如下：
1. **复制模板**：`cp .env.example .env`
2. **编辑 .env 文件**：设置 `INTEGRATION_NAME` 为你的集成名称
3. **运行 OAuth 向导**：`npm run oauth:setup` 会自动填充其他凭证
4. **添加 AI 模型密钥**：根据需要添加 `ANTHROPIC_API_KEY`、`DOUBAO_API_KEY` 或 `QIANWEN_API_KEY`

### 3.3 如何运行示例应用？

**Q**: 如何运行 Day AI SDK 的示例应用？

**A**: 运行示例应用的步骤如下：

**桌面应用**：
```bash
cd examples/desktop
npm install
npm run dev  # 运行开发服务器
npm run electron:dev  # 运行 Electron 应用
```

**移动应用**：
```bash
cd examples/mobile
npm install
npm run ios  # 运行 iOS 模拟器
npm run android  # 运行 Android 模拟器
npm run web  # 运行 Web 版本
```

**Web 应用**：
```bash
cd examples/vercel-weather-cron
npm install
npm run dev  # 运行开发服务器
```

### 3.4 如何打包桌面应用？

**Q**: 如何打包 Day AI SDK 的桌面应用？

**A**: 打包桌面应用的步骤如下：
1. **安装 electron-builder**：`npm install electron-builder --save-dev`
2. **修改 package.json**：添加打包配置
3. **构建应用**：`npm run build`
4. **执行打包**：`npx electron-builder`
5. **获取安装包**：安装包会生成在 `dist` 目录中

## 4. 故障排查

### 4.1 沙盒初始化失败怎么办？

**Q**: 运行 Electron 应用时提示 "sandbox initialization failed: Operation not permitted" 怎么办？

**A**: 解决方法如下：
1. **修改 package.json**：在 `electron:dev` 脚本中添加 `--no-sandbox` 参数
   ```json
   "electron:dev": "electron --no-sandbox ."
   ```
2. **修改 webPreferences**：在 `electron/main.ts` 中的 `webPreferences` 中添加 `sandbox: false`

### 4.2 微信聊天记录收集失败怎么办？

**Q**: 调用 `collect_wechat_messages` 工具时失败怎么办？

**A**: 解决方法如下：
1. **确保微信已安装**：确保微信已安装并登录
2. **检查权限**：确保用户有微信数据目录的访问权限
3. **安装 PyWxDump**：运行 `pip install pywxdump`
4. **检查路径**：检查微信数据路径是否正确
5. **查看日志**：查看应用日志，了解具体错误信息

### 4.3 AI 模型调用失败怎么办？

**Q**: 调用 AI 模型时提示 "API key not configured" 怎么办？

**A**: 解决方法如下：
1. **检查 API 密钥**：确保在 `.env` 文件中配置了正确的 API 密钥
2. **对应模型配置**：
   - Claude AI：设置 `ANTHROPIC_API_KEY`
   - 豆包：设置 `DOUBAO_API_KEY`
   - 千问：设置 `QIANWEN_API_KEY`
3. **检查网络连接**：确保能够访问对应 AI 模型的 API

### 4.4 OAuth 授权失败怎么办？

**Q**: 运行 `npm run oauth:setup` 时授权失败怎么办？

**A**: 解决方法如下：
1. **检查网络连接**：确保网络连接正常，能够访问 Day AI 平台
2. **在时限内完成**：确保在 5 分钟内完成浏览器授权
3. **检查集成名称**：确保 `INTEGRATION_NAME` 已正确设置
4. **重新运行**：重新运行 `npm run oauth:setup`

## 5. 高级用法

### 5.1 如何自定义工具？

**Q**: 如何在 Day AI SDK 中自定义工具？

**A**: 自定义工具的步骤如下：
1. **在 tools.ts 中添加工具定义**：在 `examples/desktop/electron/services/tools.ts` 中添加新工具的定义
2. **在 ToolExecutor.ts 中实现工具**：在 `examples/desktop/electron/services/ToolExecutor.ts` 中实现工具的执行逻辑
3. **注册工具**：确保工具被正确注册到 AgentService

### 5.2 如何集成自定义 AI 模型？

**Q**: 如何在 Day AI SDK 中集成自定义 AI 模型？

**A**: 集成自定义 AI 模型的步骤如下：
1. **创建模型客户端**：创建继承自 `LLMClient` 的模型客户端类
2. **实现模型方法**：实现 `chat` 和 `continueAfterTool` 方法
3. **注册模型**：在 `AgentService` 中注册新模型
4. **配置模型**：在 `.env` 文件中添加模型 API 密钥

### 5.3 如何实现实时数据监控？

**Q**: 如何在 Day AI SDK 中实现实时数据监控？

**A**: 实现实时数据监控的步骤如下：
1. **使用文件监控**：监控微信数据库文件变化
2. **设置监听器**：使用 Node.js 的 `fs.watch` 或第三方库如 `chokidar`
3. **处理变化**：当文件变化时，触发数据处理逻辑
4. **实时更新**：将处理后的数据实时更新到应用界面

### 5.4 如何优化应用性能？

**Q**: 如何优化 Day AI SDK 应用的性能？

**A**: 优化应用性能的方法如下：
1. **减少网络请求**：缓存频繁访问的数据
2. **优化 AI 调用**：合理设置 AI 模型的参数，减少不必要的调用
3. **使用流式响应**：使用流式响应，提升用户体验
4. **优化渲染**：使用 React 的 memo、useCallback 等优化渲染性能
5. **减少内存使用**：及时清理不需要的数据，避免内存泄漏

## 6. 最佳实践

### 6.1 如何保护 API 密钥？

**Q**: 如何保护 Day AI SDK 中的 API 密钥？

**A**: 保护 API 密钥的最佳实践：
1. **使用环境变量**：将 API 密钥存储在 `.env` 文件中
2. **添加到 .gitignore**：确保 `.env` 文件被添加到 `.gitignore`，不被提交到版本控制系统
3. **使用密钥管理服务**：在生产环境中，使用密钥管理服务存储 API 密钥
4. **定期轮换**：定期轮换 API 密钥，减少安全风险

### 6.2 如何确保数据安全？

**Q**: 如何确保 Day AI SDK 应用的数据安全？

**A**: 确保数据安全的最佳实践：
1. **使用 HTTPS**：确保所有网络通信使用 HTTPS
2. **加密存储**：对敏感数据进行加密存储
3. **访问控制**：实施适当的访问控制，限制数据访问权限
4. **数据备份**：定期备份应用数据
5. **审计日志**：记录重要操作的审计日志

### 6.3 如何提升用户体验？

**Q**: 如何提升 Day AI SDK 应用的用户体验？

**A**: 提升用户体验的最佳实践：
1. **响应式设计**：确保应用在不同设备上都有良好的显示效果
2. **实时反馈**：提供实时的操作反馈，如加载动画、成功提示等
3. **错误处理**：提供友好的错误提示，帮助用户解决问题
4. **个性化设置**：允许用户自定义应用设置，如主题、语言等
5. **性能优化**：确保应用响应迅速，避免卡顿

### 6.4 如何进行版本管理？

**Q**: 如何对 Day AI SDK 应用进行版本管理？

**A**: 版本管理的最佳实践：
1. **语义化版本**：使用语义化版本号（如 1.0.0）
2. **Git 标签**：使用 Git 标签标记重要版本
3. **发布说明**：为每个版本编写详细的发布说明，包括新功能、修复的问题等
4. **分支管理**：使用分支管理不同版本的开发和维护
5. **回滚策略**：制定版本回滚策略，以应对紧急情况

## 7. 常见问题

### 7.1 Day AI SDK 支持哪些平台？

**Q**: Day AI SDK 支持哪些平台？

**A**: Day AI SDK 支持以下平台：
- **桌面平台**：Windows、macOS、Linux
- **移动平台**：iOS、Android
- **Web 平台**：支持现代浏览器

### 7.2 Day AI SDK 需要哪些依赖？

**Q**: Day AI SDK 需要哪些依赖？

**A**: Day AI SDK 的主要依赖包括：
- **Node.js**：16.0 或更高
- **npm 或 yarn**：包管理工具
- **Git**：代码版本控制
- **Python**：3.8 或更高（用于微信聊天记录收集）
- **AI API 密钥**：根据使用的 AI 模型需要相应的 API 密钥

### 7.3 如何获取 Day AI 平台账号？

**Q**: 如何获取 Day AI 平台账号？

**A**: 访问 https://day.ai 注册 Day AI 平台账号。

### 7.4 如何获取 AI 模型的 API 密钥？

**Q**: 如何获取 Claude AI、豆包、千问等 AI 模型的 API 密钥？

**A**:
- **Claude AI**：访问 https://console.anthropic.com/ 注册并获取 API 密钥
- **豆包**：访问 https://cloud.bytedance.com/ 注册并获取 API 密钥
- **千问**：访问 https://dashscope.aliyun.com/ 注册并获取 API 密钥

### 7.5 Day AI SDK 的许可协议是什么？

**Q**: Day AI SDK 的许可协议是什么？

**A**: Day AI SDK 使用 MIT 许可协议。

## 8. 技术支持

### 8.1 如何获取技术支持？

**Q**: 如何获取 Day AI SDK 的技术支持？

**A**: 获取技术支持的方式：
- **GitHub Issues**：在 https://github.com/wlqtjl/day-ai-sdk/issues 提交问题
- **Email**：contact@day.ai
- **Discord**：Day AI 开发者社区

### 8.2 如何提交 bug 报告？

**Q**: 如何提交 Day AI SDK 的 bug 报告？

**A**: 提交 bug 报告的步骤：
1. **详细描述**：详细描述 bug 的症状、重现步骤和预期行为
2. **提供环境信息**：包括操作系统、Node.js 版本、Day AI SDK 版本等
3. **提供日志**：提供相关的日志文件和错误堆栈
4. **重现步骤**：提供详细的重现步骤，以便开发团队能够复现问题
5. **预期行为**：描述你期望的正确行为

### 8.3 如何贡献代码？

**Q**: 如何为 Day AI SDK 贡献代码？

**A**: 贡献代码的步骤：
1. **Fork 仓库**：在 GitHub 上 fork Day AI SDK 仓库
2. **创建分支**：创建一个新的分支用于你的贡献
3. **编写代码**：实现新功能或修复 bug
4. **测试代码**：确保你的代码通过测试
5. **提交 PR**：提交 pull request，描述你的贡献内容
6. **代码审查**：参与代码审查过程，根据反馈进行修改

### 8.4 如何获取最新版本？

**Q**: 如何获取 Day AI SDK 的最新版本？

**A**: 获取最新版本的步骤：
1. **拉取代码**：`git pull origin main`
2. **更新依赖**：`npm install` 或 `yarn install`
3. **重新构建**：`npm run build` 或 `yarn build`
4. **查看版本**：`npm version` 查看当前版本

## 9. 总结

Day AI SDK 是一套强大的开发套件，为开发者提供了快速构建 AI 驱动的业务工具的能力。通过本知识问答文档，你应该能够了解 Day AI SDK 的核心功能、技术实现、安装配置、故障排查、高级用法、最佳实践和常见问题等方面的知识。

如果遇到任何问题，请参考本文档的相关部分，或联系技术支持团队获取帮助。

祝你使用愉快！